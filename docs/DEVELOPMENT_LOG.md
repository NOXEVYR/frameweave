# 开发与验收记录

## 2026-09-25 · 0.5.0 GitHub 同步

用户明确要求同步上传。发布目标为 `NOXEVYR/frameweave` 的 `main`，沿用仓库 `releases/` 目录提供 Windows x64 和源码 ZIP；保留已有历史归档、下载链接和兼容身份。只同步公开源文件允许清单、文档、图标、截图及 0.5.0 发行文件，不包含本机模型、配置、生成结果、测试数据或日志。

程序沿用 9 月 24 日已验证的 EXE，不重新构建。README 切换到 0.5.0 下载和 PrismCanvas.exe 入口，补充品牌与迁移记录；两份 ZIP 因内附文档更新而重新封装，包修订标识为 publication-20260925。重新核对源码清单、ZIP CRC、EXE 摘要和独立解压启动；发布后按固定提交回检远端树和两个下载包。既有 189 项 Python、25 项 Node、48 项源码页面、22 项打包页面验收证据保持，原生任务栏、新 GPU 推理与外部 AI 产品未验收的边界不变。最终提交身份及下载回检结果保存在独立同步报告中。

## 2026-09-24 · 0.5.0 棱光品牌与工作台改版

用户选定第一排第四个 E04「棱光切片」作为画布图标，授权按图标调整名称、页面布局、配色和 UI。显示品牌改为「棱光 PrismCanvas」；E01 的 AI_Hub 品牌与页面改版已转交用户指定的现有任务独立执行。映序的 E03 仅记录为可能方向，保留其当前黑白视觉与图标，不跨项目擅自替换。

本轮使用钴蓝、杏桃和暖灰，区分全局功能与项目操作，重新组织创作工具、中央画布和节点参数/生成队列。E04 原始 SVG 保留，将同一图源用于页面标志、favicon、PNG 和多尺寸 ICO。界面、图标、验收分别由子代理完成，主代理处理显示文案、兼容边界、集成与便携包；开发记录随验收更新。

兼容性：默认 FrameWeave 数据目录、Python 包、HTTP API、MCP server name、浏览器存储键、JSON schema、工作流包格式和后端输出前缀均保留。没有迁移或清理个人数据、模型、已保存画布或旧发行包。新 Windows 入口使用 PrismCanvas.exe；既有 0.4.0 与更早包保留可回退。此改版不更换推理算法，不把模拟生成测试称作新的 GPU 或画质验证。当前仅本地交付，公开仓库与公开下载未随本次改动发布。

已验证：189 项 Python、25 项 Node、Python 编译、JavaScript 语法和兼容 CRLF 的差异格式检查。源码页面共 48 项：五宽度品牌/布局 15、原交互回归 17、AI/外部任务交互 7、竖图与边界布局 4、MCP 联动 5。最后文案与对比度调整后重跑 15 项布局。EXE 页面另通过 17 项交互和 5 项 MCP 联动；JS 错误为零。数据工作流导入、单次提交、结果入画布、撤销/重载和已有 MP4 的播放/关闭清理均经过实际页面操作。

图标校验：同图源 SVG、512 PNG、16/20/24/32/40/48/64/128/256 九帧 ICO；Windows 资源 API 只读提取 EXE，九帧与正式 ICO 逐字节匹配。EXE 为 9,558,578 字节（约 9.12 MiB），较 0.4.0 增加 37,183 字节，无新运行依赖。页面用独立系统 Edge 上下文检查；包内 UI 验收仅拦截环境扫描请求，任务与 MCP 请求仍经真实 EXE 及隔离 mock，未扫描个人推理安装。静态前端及图标随包交付，公开截图使用隔离测试后端。

边界：本轮未做新的真实 GPU 推理、外部 AI 产品配置或 Windows 原生窗口/任务栏实机验收，不据此宣称画质、算法速度或任务栏缓存已改变。最终 ZIP 的 CRC、SHA-256、源文件清单及独立解压启动结果另存交付验证记录；旧 0.3.0、0.4.0 包保留。

## 2026-09-24 · 图标第二轮创意重做

用户认为第一轮多数图标缺乏创意，要求继续设计。用户先提及 D05，随后撤回刚刚的偏好问答选择，并指示由助手自行决定。本轮不锁定原配色或双框形状，也不将任何候选视为最终定版。

重新绘制 12 个矢量候选，分时间流动、空间错觉与生长动势，使用更明显的非对称剪影、弧形负空间、分裂与折光。保留第一轮；程序现有图标与 0.4.0 包不改动。本轮内置图像生成仍连接失败，实际交付为 SVG 及浏览器渲染的 PNG 展示板；未启用外部 API。

主代理负责 E 组、交付与记录；两个子代理负责 F、G 组。12 份 SVG 的 XML、尺寸、渐变引用与独立摘要通过检查，无外部资源。实际浏览器验证 12 图全部加载、分组筛选、放大和四档尺寸预览、单选、下载目标与深浅底切换；1440/1024/600 宽度无横向溢出，JS 错误为零。生成总览和三张分组图，并目视检查整体及小尺寸辨识。最终 ICO、程序替换与真实任务栏验收仍等待用户选定。

## 2026-09-24 · 图标候选设计（等待用户选择）

用户要求重新设计图标，提供大量方案自行选取。本轮制作 24 个原创可编辑 SVG，分几何负空间、柔性编织、光影帧层、立体材质四组，编号 A01–D06，保存在独立的图标候选交付目录；附交互选图页、总览与四张分组大图。

内置图像生成服务四次调用均连接失败，没有返回图片。向用户说明后改用矢量绘制，未启用另需密钥的 API 路径。主代理负责 A 组及整合，子代理分别完成其他组；不把矢量渲染描述为成功的材质出图。

24 个 SVG 的结构、渐变、尺寸及独立摘要通过检查；实际浏览器验证筛选、放大、多尺寸预览、选择、下载目标和深色展示，1440/1024/600 宽度无横向溢出，JS 错误为零。完成总览与小尺寸目视检查。尚未选定最终图标，原应用图标与 0.4.0 成品保留，不提交发布；ICO、多尺寸像素优化及任务栏等实际显示将在用户选定后处理。

## 2026-09-24 · 0.4.0 界面与 AI 调用入口（本地开发）

用户本轮要求全面改善界面，并让 AI 能方便调用软件。保留此前未提交的账号链接同步改动，使用三个子代理并行处理视觉、交互和 MCP 模块；主代理负责 HTTP 集成、独立检查、文档和本地交付。历史版本、模型、个人配置与画布不覆盖，本轮不提交或发布 GitHub。

实现：统一深蓝青绿工作台、紧凑工具栏、节点和参数面板、队列、包库与弹窗；新增节点和复用片段寻找空位；队列任务可放入或定位到画布，保留撤销与草稿。网络提交期间切换画布不再错误绑定已删除节点。

AI 接入采用标准库实现的本机 Streamable HTTP MCP，提供 14 个可发现工具，复用既有环境、工作流、图像/视频生成与任务接口。没有增加运行依赖、下载模型或提供任意命令执行。连接配置仅在用户打开弹窗时读取，关闭后清除；未写入浏览器持久存储或项目导出。MCP 连接必须位于同一台电脑，启动令牌变化需重新复制配置；未宣称已经替用户配置了任何外部 AI 客户端。

可靠性：MCP 生成及重试前持久化请求 ID，并以跨进程文件锁防止双开客户端同时提交。响应丢失或中断后保持不确定状态，不自动重发；同键异参拒绝；同键已接受任务即使离开最近列表也不重新生成。重试按原任务隔离请求 ID，与生成分别记录，最多保存 2000 个操作；达到上限会停止新增，不能通过删除记录处理未核实的提交。HTTP 校验 Host、Origin、Bearer、协议版本、媒体类型、UTF-8 与大小；没有放宽原有写接口。旧 HTTP 重试入口的跨实例状态同步不属于此次 MCP 去重改动范围。

已验证：189 项 Python（含 40 项 MCP 工具和 11 项 HTTP 传输）、25 项 Node、Python 编译、JavaScript 语法和差异格式检查。源码与 EXE 各 17 项原有界面回归；源码另有 7 项新交互、4 项布局回归；源码和 EXE 各 5 项 MCP 联动检查。均为独立系统 Edge 上下文，未读取个人浏览器资料，JS 错误为零。隐藏事件另各 1 项模拟验证，不冒充原生窗口遮挡验收。

审阅发现并修复：竖图结果卡的初始尺寸偏小，加载媒体后可能压住相邻节点；边界坐标上的复用片段在布局前越界；同后端任务 ID 在切换后端后可能与旧请求混淆；双实例同时首次提交 MCP 重试可能重复排队。接口拒绝非 UTF-8、无效 Unicode、非有限数字和无 ID 的工具执行。新增按钮的无障碍名称跟随可见动作变化。

成品使用已有 PyInstaller 构建，EXE 为 9,521,395 字节（约 9.08 MiB），仅比 0.3.0 增加 30,590 字节。保留旧版归档，使用文件允许清单生成新版本 ZIP。归档 CRC、SHA-256、独立解压启动、14 个工具离线发现和嵌入静态文件一致性列入本地交付检查。

生成算法保持原样，本轮使用模拟后端与已有视频验证数据链路、媒体播放和界面，不属于新的真实 GPU 推理或画质提升。尚未连接第三方 AI 产品做产品专属配置验收，未推送或发布 GitHub。

## 2026-09-24 · 本机同步与账号链接核对（未发布）

本轮仅核对并同步本机 FrameWeave，不新增功能、不提交或发布 GitHub，不处理其他工具。实时仓库接口确认仓库为 `NOXEVYR/frameweave`，账号数字 ID 为 `94962603`，与原账号一致；main 仍指向已验证的 0.3.0 提交 `906444d17a989d32315fb9065725e28af78e1204`。

GitHub latest Release 当前仍是“FrameWeave 0.2.0 · 下载入口”，无二进制附件；仓库 releases 目录中的最新已发布便携包为 0.3.0。两种入口分别核对，未按旧 Release 页面降级本机。新账号下的 0.3.0 Windows / 源码下载链接及 0.2.0 历史 Windows 链接均返回 HTTP 200。

本机现有 0.3.0 EXE 为 9,490,805 字节，SHA-256 与远端发行清单一致；没有匹配的运行进程。未发现额外安装入口，沿用现有便携目录。源码及便携目录的 README/MIGRATION 链接改为 NOXEVYR，修改前已保存逐文件备份和摘要；程序代码、EXE、历史 ZIP、模型、配置、画布和任务数据未改动，没有新增下载。程序当前无自动更新器或内置 GitHub 更新地址，无需修改用户配置。

本轮验证范围为固定提交的远端清单、本地 EXE/ZIP 摘要与归档完整性、链接可达性及隔离启动。不重复宣称此前真实 GPU 生成或完整 UI 验收是本轮新完成的检查。GitHub 页面中旧账号链接和旧 Release 入口仍需后续获得发布授权后更新；本轮本地文档修改保留为未提交差异。

## 2026-09-18 · 0.3.0 日常生成流程优化

需求：继续同步优化图片、视频画布的其他功能，并记录开发与验收过程。

当前决定：增加历史任务参数复用、明确操作的再次生成、状态筛选和搜索；工作流包增加收藏、归档与恢复；减少队列刷新时重复创建媒体，统一预览关闭与隐藏时的播放清理。保持已有画布和包内容 ID 兼容。

分工：包库后端、前端交互、只读风险审核由三个子代理并行；主代理负责任务接口、集成、发布与记录。生成参数和任务记录只存本机用户目录，归档不删除工作流或模型。

发布目标已核实：独立仓库 `turnsolesama/frameweave`，以当前远端提交为基础保留迁移说明和已有修改；portfolio 历史下载地址继续保留。

发现并纳入修复：后端接受任务后本地记录写失败可能被误报成提交失败；再次生成需要防止并发与响应丢失造成重复提交。重试使用已保存的精确 API 图，并重新校验当前后端节点。

已验证：138 项 Python、21 项 Node、语法检查；源码与最终 EXE 各 17 项实际界面检查，另各 1 项模拟可见性事件检查。真实 Krea 图片约 14.25 秒、H3 文生视频约 143.98 秒，均验证精确图重试与同请求去重；后端缓存耗时单列，不作为推理提速依据。视频完整解码与抽帧通过，音轨未做主观评分。

并行审查发现并修复：数字框清空被写成零、越界后 UI 与实际参数不同；H3 高级 shift/ref_image_size 和顶层 LoRA 在参数复用中丢失；近 4 MiB 记录的计量必须与实际缩进落盘格式一致。媒体关闭检查需等待浏览器异步 close 事件，500 ms 内确认暂停、清空源并移除元素，不能只检查弹窗隐藏。

成品使用既有 PyInstaller 构建，没有新增模型、运行库或浏览器下载。便携包和源码包通过允许清单打包；独立仓库发布后按不可变提交回检文件与下载 ZIP 的 SHA-256、CRC 和目录结构，证据记录在发行清单与本地发布记录。

保留边界：真实窗口遮挡/隐藏未做原生桌面验收；历史复现图不自动回收，4 MiB 是单次记录上限；重试结果默认放在当前画布中央，复杂布局可能需要手动拖开。没有新增完整视频剪辑器或推理引擎。

## 2026-09-11 · 0.2.0 功能与本地验收完成

本轮目标：自动识别本地推理环境和缺失项；将图片、视频 API 工作流封装成可填写参数的工作流包；保持客户端轻量、界面响应及时，并验证实际生成链路。

分工：环境发现、工作流诊断、前端画布与包表单由三个并行子代理负责；主代理负责包存储、输入校验、服务整合、生成链路验收与发布。

### 范围与实现决策

- 自动探测设置地址、少量常用端口以及本机 ComfyUI 进程提供的候选地址，不扫描整盘或局域网。
- 区分确认缺失、检测异常、待核实、暂不可检查和已通过，后端离线不等于所有模型与节点都缺失。
- 工作流包采用有版本标记的 JSON，保存 API 图和参数映射；不包含模型、可执行脚本或媒体文件。模型与节点仍由已有 ComfyUI 提供。
- 开放参数可包含提示词、整数、数值、开关、下拉选项和参考图上传；运行前使用实时节点定义校验。
- 普通 ComfyUI 画布 JSON 需要先导出 API 格式；本轮不猜测 UI 小部件与节点输入的映射。
- 包与用户参数保存在本机数据目录，不进入公开源码、发行包或测试记录。
- 现有模型、参考软件、历史版本和下载地址保留。自动检测只读；不自动下载安装、执行第三方安装脚本或重建已有环境。

### 当前进度

- 已完成：复查 0.1.0 源码、现有检测与测试命令；确认 Git 工作区初始干净。
- 已完成：工作流包后端，包括结构检查、参数映射、值校验、内容指纹、导入导出与本地存储。
- 已完成：环境发现、诊断分类、包库界面、后端接口整合与打包。
- 已完成：109 项 Python、16 项 Node、源码与 Windows EXE 各 17 项界面检查，以及 5 项打包版真实表单、结果与播放检查。
- 已完成：Krea 图片、H3 文生视频、H3 首帧图生视频通过包生成；打包版另外通过表单提交新提示词和种子，返回真实图片。
- 发布要求：上传后核对公开文件与重新下载的归档；具体提交、包大小与 SHA-256 记录在发行清单及交付记录中。保留 0.1.0 历史归档。

### 已发现问题

- 0.1.0 后端离线时仍将未读取到的节点和模型标为缺失；本轮改为未知，避免重复安装。
- 原诊断入口只传模式和模型，不能完整识别参考图、LoRA 与 API 工作流依赖；本轮传完整生成请求。
- 包参数不得覆盖连线、绑定不存在的节点、包含重复映射或非有限数字；JSON 包仅为数据，导入不会运行生成。
- 并行审核修复：深层损坏 JSON 导致请求断开、超大数值边界溢出、种子超出浏览器整数精度、上传途中切后端导致错误归属、扫描期间设置变化导致旧缓存重新生效。
- 并行审核修复：推荐字段名称不一致、参考图字段可取消导致包无法输入图片、前端重新拼接本地检查明细导致脱敏文本回流。
- 共享模型根目录会让嵌入、ControlNet、视觉编码器出现在主模型候选中；已排除这些明确不适用的目录角色，未知自定义模型仍需实际加载验证。
- 验收脚本曾误用 `images` 而非协议要求的 `references`，编译层正确阻止无参考图请求；修正脚本后首帧图生视频完成。界面测试曾用相对地址匹配浏览器解析后的绝对地址，造成假超时；截图和 DOM 确认图片已正确显示，修正断言后通过。这两项不是产品生成失败。

### 结果与性能证据

- 自动环境扫描约 1.4 秒，识别测试后端、关联 Python 与 6 项依赖元数据。未启动的自定义安装仍可能需要用户填写安装目录。
- RTX 5070 Ti 16 GB：Krea 512×512、8 步约 16.3 秒；H3 文生视频约 116.9 秒；H3 首帧图生视频约 120.9 秒，两段视频均 768×448、124 帧、24 fps、5.167 秒。
- Windows EXE 的首次表单图片生成约 12.1 秒。重复相同图命中后端缓存，只用于界面回传验证，不作为推理速度数据。
- 两段视频完整 FFmpeg 解码通过，抽帧能识别茶壶、桌面、光线；图生视频保留参考主体并出现蒸汽。浏览器播放时间推进正常，音轨只检查结构，未评分音质或音画同步。
- 100 节点、66 连线的离屏浏览器检查：源码导入至首次缩放约 130 ms，EXE 约 93 ms；21 次缩放与 16 步拖动序列分别约 562 / 575 ms。包含测试工具开销，不代表桌面 FPS 或其他设备性能。

### 保留的边界

本轮不实现自动安装、完整独立推理引擎、UI 格式工作流通用转换、视频编辑时间线或长片质量保障。H3 双参考首尾帧、Ref2VA、多角色/Turbo 质量、Krea 多图编辑、SDXL 不同模型和其他显卡仍需专项真实验证。检查通过只说明对应范围，不能替代生成与质量验收。

本文件随里程碑更新；已通过、模拟验证、实际 GPU 验证和未验证项分别记录，不以接口可达代替生成验收。

## 2026-09-12 · 统一工具用途说明

- 首页明确图片与视频生成画布，以及本地 ComfyUI 环境发现、依赖检查与后端状态范围。
- 仅修改介绍文案；下载地址与应用代码保持原样。核对已实现的环境发现、工作流检查和生成接口，发布后回读文档确认。没有新增运行测试或实机效果验收。
