# 验证记录

## 0.7.0 · 2026-09-26

238 项 Python、71 项 Node、Python 编译和全部前端 JS/MJS 语法检查通过。源码与 Windows EXE 各通过 108 项页面检查，未捕获 JavaScript 异常：

| 范围 | 每端项数 | 证据边界 |
| --- | ---: | --- |
| 画布与独立生成主要操作 | 15 | 隔离回环模拟后端，实际上传、编译、图片解码和已有 MP4 播放 |
| 丢失响应、请求恢复与存储故障 | 6 | 注入网络/存储失败，核对原 ID 与冻结参数 |
| 缩放、框选、对齐、复制、撤销与快捷键 | 17 | 实际页面几何，阻断生成调用 |
| 独立工作台草稿与媒体边界 | 11 | 浏览器合成状态、上传与媒体响应 |
| 文生图包 → 图生图包 → H3 视频包 | 8 | 模拟后端顺序执行，检查真实提交图、上传输入、唯一请求及失败阻断 |
| 集合与单包无损导入导出 | 12 | 两个独立数据目录，空包库恢复、原包 ID 保持、非法集合拒绝，无生成 |
| 导入锁与跨画布身份 | 5 | 同 ID 节点、撤销/重做、恢复旧运行，不误挂结果 |
| 原任务队列、包库与预览回归 | 17 | 独立浏览器与模拟后端；另有 1 项模拟页面隐藏事件，不计入 108 |
| MCP 初始化、14 工具、去重与结果入画布 | 5 | 真实 HTTP 和成品版本 0.7.0，模拟推理 |
| 工具条与运行卡布局 | 12 | 390/700/800/1024/1280/1600 宽，展开/折叠状态，实际相交与可滚动性检查 |

成品画布首轮在并发测试启动时等待引擎超时，尚未进入第 1 项；未修改源码或脚本，单独完整重跑 17 项通过。原超时记录保留在私有测试目录。其余最终结果已分别记录，不把重试失败隐去。

EXE 为 9,607,672 字节，约 9.16 MiB；全部 15 个内嵌前端文件与构建源码逐字节一致，无新运行依赖。100% 画布和独立生成页截图已目视检查，使用示例包与空结果视图。最终 ZIP 另检查 CRC、SHA-256、源文件清单、旧归档未改变及独立解压启动，结果见 `releases/release-manifest-v0.7.0.json`。

个人 8188 推理服务本轮检查未响应。未启动或改动个人推理服务、模型及配置，没有新增真实 GPU 推理、速度、画质或原生窗口/任务栏验收结论。测试只在隔离数据目录执行；环境扫描被拦截。旧版与原桌面快捷方式备份保留。初次完成本地验收后，用户要求同步 GitHub。发布沿用已验证 EXE，因公开说明更新而重新封装 ZIP，并按固定提交回检远端内容与下载包。

## 0.6.0 · 2026-09-25（本地候选版）

206 项 Python、45 项 Node、Python 编译、JavaScript 语法检查通过。源码与新 Windows EXE 各通过 49 项页面检查，未捕获 JavaScript 异常：

| 范围 | 项数 | 证据边界 |
| --- | ---: | --- |
| 双击建节点、独立生成表单、模型/LoRA、图生图上传编译、图片/视频回传、布局与重载 | 15 | 真实客户端 + 隔离模拟后端，播放已有 MP4 |
| 不确定提交、原 ID 与参数恢复、明确拒绝、存储故障 | 6 | 真实客户端 + 隔离模拟后端，并注入丢失响应 |
| 150%/200%/300% 缩放端点、菜单、框选对齐、内部连线复制、撤销与快捷键隔离 | 17 | 独立浏览器上下文，无生成调用 |
| 子模式草稿保留、DiT/LoRA 前置选择、同页播放与切页暂停 | 11 | 状态/任务/上传/媒体均为浏览器合成响应，无生成调用 |

文字与布局已目视检查；700、1280、2056 像素宽度无横向溢出。150%–300% 按真实端口中心检查连线；纯平移验证复用原 SVG 元素。EXE 内嵌的全部 12 个前端文件与构建源码逐字节一致。环境扫描在 UI 验收中被拦截，不扫描个人安装。

EXE 为 9,585,679 字节，约 9.14 MiB，不捆绑浏览器、模型或推理环境。最终归档另核对 ZIP CRC、SHA-256、源文件清单及解压后独立启动；对应清单见 `releases/release-manifest-v0.6.0.json`。旧包保留，桌面快捷方式备份后更新。截图展示隔离目录和示例画布，不作为真实生成证据。

本轮个人推理后端未启动，因此没有新增真实 GPU 生成、算法速度、LoRA 画质或跨模型兼容性结论；Windows 原生窗口与任务栏显示也未做实机观察。本轮仅本地交付，公开下载仍是 0.5.0。

## 0.5.0 · 2026-09-24（棱光本地版）

已验证：189 项 Python、25 项 Node、Python 编译、JavaScript 语法和兼容 CRLF 的差异格式检查。源码页面共 48 项：五宽度品牌/布局 15、原交互回归 17、AI/外部任务交互 7、竖图与边界布局 4、MCP 联动 5。最后文案与对比度调整后重跑 15 项布局。EXE 页面另通过 17 项交互和 5 项 MCP 联动；JS 错误为零。数据工作流导入、单次提交、结果入画布、撤销/重载和已有 MP4 的播放/关闭清理均经过实际页面操作。

图标校验：同图源 SVG、512 PNG、16/20/24/32/40/48/64/128/256 九帧 ICO；Windows 资源 API 只读提取 EXE，九帧与正式 ICO 逐字节匹配。EXE 为 9,558,578 字节（约 9.12 MiB），较 0.4.0 增加 37,183 字节，无新运行依赖。页面用独立系统 Edge 上下文检查；包内 UI 验收仅拦截环境扫描请求，任务与 MCP 请求仍经真实 EXE 及隔离 mock，未扫描个人推理安装。静态前端及图标随包交付，公开截图使用隔离测试后端。

边界：本轮未做新的真实 GPU 推理、外部 AI 产品配置或 Windows 原生窗口/任务栏实机验收，不据此宣称画质、算法速度或任务栏缓存已改变。最终 ZIP 的 CRC、SHA-256、源文件清单及独立解压启动结果另存交付验证记录；旧 0.3.0、0.4.0 包保留。

## 0.4.0 · 2026-09-24（本地版）

本轮完成工作台美化与本地 MCP 接口，没有改动生成算法或模型。189 项 Python 测试、25 项 Node 测试、Python 编译和 JavaScript 语法检查通过；其中新增 40 项 MCP 工具测试和 11 项实际 HTTP 传输测试。

MCP 验证包括初始化与工具发现、严格输入校验、8 种原生图片/视频请求编译、无副作用包导入、参考图上传、任务查询与取消、同键异参拒绝、跨实例锁、重启与并发去重、网络响应不确定、落盘失败、UTF-8/Host/Origin/Bearer 与协议头验证。使用模拟推理服务，不据此推断显卡性能或模型画质。

源码和 Windows EXE 各通过 17 项原有界面回归与 5 项 MCP 联动检查；源码另通过 7 项新增交互和 4 项布局回归。已检查 1024/1280/1600 宽度的主操作入口、画布与面板；AI 配置可以复制且关闭后从 DOM 清除，不进入画布导出或本地存储。MCP 创建的任务只提交一次，结果进入同一队列，可放入/定位画布，并实际播放已有 MP4。所有上述浏览器检查的 JS 错误为零。

竖图异步加载和多输出结果不再因初始高度估计不足而重叠；合法坐标边界的双参考图参数复用可保存重载。沿用旧版草稿、包库归档、重试、筛选、媒体 DOM 保留、关闭/Esc/预览切换/真实导航清理检查。页面隐藏另记为模拟事件；没有把离屏浏览器测试称为原生窗口遮挡验收。

EXE 为 9,521,395 字节，约 9.08 MiB。归档及对应源码清单用 SHA-256 和 ZIP CRC 核对；从新 ZIP 独立解压启动，验证版本、无后端时发现 14 个工具、空任务列表与内嵌前端一致性。旧 0.3.0 Windows ZIP 保持原摘要。

本轮没有重新执行真实 GPU 推理；相关历史证据在下文。第三方 AI 产品的实际 MCP 配置未分别验收，接口没有自动写入用户的 AI 客户端设置。本轮成品未发布 GitHub。界面截图展示示例画布，不能作为生成已成功的证明。

## 0.3.0 · 2026-09-18

本轮重点是任务复现、队列与包库操作、输入保存和媒体生命周期。生成算法、模型与采样器实现仍由外部 ComfyUI 提供。

### 服务与真实生成

最终 138 项 Python 测试、21 项 Node 测试、Python 编译与 JavaScript 语法检查通过。源码服务与 Windows EXE 各通过同一套 17 项实际界面检查，控制台错误为零：数字输入回退、未失焦草稿、包库整理、重启保留、归档包生成、独立参数复用、重试去重、搜索筛选、轮询保留媒体元素、播放后关闭/Esc/切换预览与真实导航退出。后端为回环模拟服务，视频素材是已生成的本地 MP4，未把这些 UI 检查冒充 GPU 生成。

页面隐藏的 visibilitychange 分支另做 1 项模拟事件覆盖；离屏 Edge 不能验证真实窗口遮挡状态，因此不把它计入真实 UI 检查。1280 宽包库与队列截图已检查，控件无横向溢出。Windows EXE 另恢复本轮真实图片/视频的 4 条历史任务并加载实际图片，首页截图来自该实例。

新增检查覆盖包整理信息持久化、导出不泄露本机整理状态、归档后仍可运行、并发部分更新；覆盖旧任务恢复、精确 API 重试、持久请求 ID、并发去重、后端改变、缺失参考图、超大记录、整数精度、磁盘写入故障和响应丢失。静态/模拟检查不代表 GPU 推理速度。

在已有 ComfyUI 0.30.0、RTX 5070 Ti 16 GB 上，确认队列空闲后顺序提交两项新任务，无新增模型下载：

| 路径 | 参数 | 本轮结果 |
| --- | --- | --- |
| Krea 图片 | 512×512，8 步，CFG 1，固定新种子 | PNG，约 14.25 秒 |
| H3 文生视频 | 768×448，20 步，124 帧，24 fps | H.264 + AAC MP4，5.167 秒，约 143.98 秒 |

两项任务均验证：保存并读取参数、再次生成的 API 图与原图完全相同、同一个请求 ID 返回同一新任务。相同图的再次生成命中引擎缓存（约 0.01–0.03 秒），只证明复现与去重链路，不计入推理性能。输出视频通过 FFmpeg 完整解码；图片和视频抽帧可见靛蓝茶壶、木桌与窗光，不据此宣称普遍画质或主观音质改善。

### 本轮保留的范围

未新增跨显卡/跨平台、长片或 Turbo 质量验证；H3 首尾帧与 Ref2VA、Krea 多图编辑等专项边界沿用下方历史记录。本轮未改动原有推理服务、模型或用户项目。UI 自动化使用独立浏览器上下文，不读取个人浏览器资料；真实页面交互与模拟可见性事件分别记录。

## 0.2.0 · 2026-09-11

109 项 Python 测试、16 项 Node 测试、Python 编译与 JavaScript 语法检查通过。源码服务与最终 Windows EXE 分别通过同一套 17 项离屏界面检查；最终 EXE 另通过 5 项实际表单提交、图片回传、大图预览、真实 H3 视频元数据和播放检查。没有浏览个人浏览器资料。

新增覆盖：离线未知与确证缺失、端口有界探测和总超时、GPU/依赖元数据、工作流包输入映射、图片必填与可移植性、稳定包 ID、损坏 JSON、整数精度、跨站写保护、上传与切换并发、无副作用导入、包任务视频回传、画布恢复以及脱敏导出。

| 真实路径 | 参数 | 结果 |
| --- | --- | --- |
| Krea 图片工作流包 | 512×512，8 步，CFG 1，euler/simple | PNG，约 16.3 秒 |
| H3 文生视频工作流包 | 768×448，20 步，124 帧，24 fps | MP4，5.167 秒，约 116.9 秒 |
| H3 首帧图生视频工作流包 | 上传本轮 Krea 参考图，768×448，20 步，124 帧 | MP4，5.167 秒，约 120.9 秒 |
| Windows EXE 表单生成 | 填入新的图片提示词和种子，512×512，8 步 | 正确提交包参数，PNG 回到画布，首次约 12.1 秒 |

测试硬件仍为 RTX 5070 Ti 16 GB，使用已有模型与原生节点的独立本机测试服务；没有新增模型下载。两段 H3 输出均 H.264 + AAC、768×448、24 fps、124 帧，FFmpeg 完整解码无错误。图生视频抽帧确认参考茶壶保留、出现蒸汽；音轨只检查结构。最终界面复验的重复请求命中后端缓存，不计为新的模型速度数据。

环境发现约 1.4 秒；源码与 EXE 的 100 节点、66 连线画布导入至首次缩放分别约 130 / 93 ms，21 次缩放与 16 步拖动序列约 562 / 575 ms。属于系统 Edge 离屏测试，包含测试工具交互，不是桌面 FPS，也不代表其他硬件表现。

未覆盖：H3 双参考首尾帧与 Ref2VA、Turbo 和长片质量、Krea 多图编辑、SDXL 各第三方模型实测、其他操作系统/GPU、模型完整官方哈希、主观音质与音画同步。详细实施与问题修复见 [开发记录](DEVELOPMENT_LOG.md)。

## 0.1.0 历史记录

验证日期：2026-09-11。代码、模拟服务、真实 GPU 推理和桌面打包分别记录。测试不会改动原参考软件的代码、配置、历史或模型。

## 实际生成

验证设备：Windows x64、NVIDIA RTX 5070 Ti 16 GB；已有 ComfyUI 0.30.0 / PyTorch 2.10.0+cu130。本次没有新增模型下载。

| 路径 | 参数 | 实际结果 |
| --- | --- | --- |
| Krea 2 文生图 | 512×512，8步，CFG1，euler_ancestral / sgm_uniform，seed 314159 | 通过客户端 `/api/jobs` 提交并回传 PNG，约14.4秒；图片内容与茶壶、木桌、窗光提示相符 |
| MiniMax H3 文生视频 | 768×448，20步，CFG1，124帧，seed 314159；pruned INT8 + NVFP4编码器 | 通过客户端提交并回传MP4，约119.1秒；H.264、24fps、124帧、5.167秒；AAC、32kHz双声道 |

H3 完整文件经 FFmpeg 无错误解码，抽取画面确认有可辨识的茶壶、桌面和光线。音轨结构通过检查；未进行主观音质评分或音画同步评分。

耗时是本客户端观测到的单次任务时间，含模型调度/加载影响和约两秒轮询误差。两种模型的题材、尺寸和参数不同，不能作为模型间速度排名，也不构成对其他机器的承诺。

Krea 发布默认按官方当前模板使用8步、CFG1、euler/simple；本次实测额外覆盖 euler_ancestral/sgm_uniform 组合。默认组合和复杂参考编辑不能仅由这一次图像生成判定质量。

## 自动验证范围

最终统计：**46 项 Python 测试、8 项 Node 核心测试、15 项离屏浏览器交互检查通过**。语法检查通过。打包后的 Windows EXE 再次通过相同15项界面检查，包括真实H3视频元数据、播放时间推进和媒体回传。

Python 测试包含工作流模式与引用、动态节点输入、模型角色、枚举/连线类型/循环、非法数据、模型文件截断、未知dtype、修复说明路径脱敏、Host/Origin/CSRF、上传、任务归属、恢复和取消隔离。

媒体测试覆盖 Range 字节一致与真实流式响应，确认在模拟后端尚未发送完视频时客户端就能返回首段。

界面检查涵盖画布拖动/缩放、创建/复制/撤销/重做/删除、JSON导入导出、自动保存、两类剪贴板复制、当前环境检查、参数编译、null进度的排队任务以及1280像素视口。测试使用独立离屏浏览器，没有读取个人浏览器资料。

Windows程序为8,874,007字节（约8.46MiB），不包含模型或浏览器。原推理后台在后续打包验收后离线，额外的打包版Krea再生成测试未完成；此前源程序通过同一HTTP接口完成的两次真实生成已记录在上表。

另外使用当前后端 `/object_info` 验证 H3 文生、首尾帧、Ref2VA、Krea文图、参考编辑、SDXL与双时钟七条 API 图。**静态 schema 通过不代表对应模式已经在 GPU 上生成成功。**

## 尚未完成的验证

- H3 首尾帧、Ref2VA、多角色一致性、Turbo LoRA 品质和长片稳定性。
- Krea多图编辑和SDXL在各种第三方模型上的画质/兼容性。
- 不同显卡、操作系统、量化方式和16GB以下显存。
- 整个模型文件的官方 SHA-256 对比；当前只做存在、角色、张量结构和长度检查。

打包体积、归档哈希与最终测试统计见发行目录的 `release-manifest.json`。初版不以尚未验证的能力作性能或质量承诺。
